# Question 1
# Write a function to print 'hello_USERNAME!' USERNAME is the input of
# the function. 

def hello_name(user_name):
    """Display a simple greeting to the user, username in all caps"""
    print("hello_" + user_name.upper() + "!")

hello_name('kbieler')


# Question 2
# Write a python function, first_odds that prints the odd numbers from 1 to 100
# and returns nothing.

def first_odds():
    """prints all odd numbers from 1 to 100"""
    for num in range(1, 100):
        if num % 2 == 1:
            print (num)
       

# Question 3
# Please write a Python function, max_num_in_list to return the max number
# of a given list.

def max_num_in_list(a_list):
    """Determines the max number of a given list"""
    a_list.sort()
    print(a_list[-1])

a_list = [13, 42, 75, 4, 83]
max_num_in_list(a_list)


# Question 4
# Write a function to return if the given year is a leap year.
# A leap year is divisible by 4, but not by 100
# unless it is also divisible by 400. 
# The return should be a boolean type (true/false)

def is_leap_year(a_year):
    """Determines whether a year is a leap year (T/F)"""
    a_year = int(a_year)
    if a_year % 4 == 0:
        return True
    else:
        return False

is_leap_year("2022")


# Question 5
# Write a function to check to see if all numbers in a list are consecutive numbers.
# The return should be boolean Type.

def is_consecutive(a_list):
    """Determines whether a series of numbers are consecutive numbers (T/F)"""
    
    
     